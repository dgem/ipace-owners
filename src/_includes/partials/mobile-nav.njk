<nav id="mobile-nav" class="mobile-nav" aria-label="Mobile navigation" aria-hidden="false">
  <ul class="mobile-nav__list" role="list">
    {% for item in navigation.public %}
      <li>
        {% if item.highlight %}
          <a
            href="{{ item.url }}"
            class="mobile-nav__link mobile-nav__link--highlight"
            data-nav-link
            data-requires-guest
          >{{ item.label }}</a>
          <a
            href="/account/"
            class="mobile-nav__link mobile-nav__link--highlight"
            data-nav-link
            data-requires-auth
            style="display:none"
          >Account</a>
        {% else %}
          <a
            href="{{ item.url }}"
            class="mobile-nav__link"
            data-nav-link
          >{{ item.label }}</a>
        {% endif %}
      </li>
    {% endfor %}
  </ul>

  <div class="mobile-nav__identity">
    <a href="/account/" class="btn btn--sm btn--secondary full-width" data-requires-auth style="display:none">Account</a>
    <button id="identity-mobile-login-btn" class="btn btn--sm btn--secondary full-width" type="button">Sign in / Register</button>
    <button id="identity-mobile-logout-btn" class="btn btn--sm btn--ghost full-width" type="button" style="display:none">Sign out</button>
  </div>
</nav>
