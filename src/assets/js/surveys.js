(function () {
  'use strict';
  function token() { var u = window.firebase && window.firebase.auth().currentUser; return u ? u.getIdToken() : Promise.reject(new Error('Sign in first.')); }
  function request(path, method, body) { return token().then(function (t) { return fetch(path, { method: method, headers: { 'Authorization': 'Bearer ' + t, 'Content-Type': 'application/json' }, body: body ? JSON.stringify(body) : undefined }).then(function (r) { if (r.status === 204) return {}; return r.json().then(function (d) { if (!r.ok) throw new Error(d.error || 'Request failed.'); return d; }); }); }); }
  function esc(v) { var d=document.createElement('div'); d.textContent=v||''; return d.innerHTML; }
  function setupAdmin(root) {
    var form=root.querySelector('[data-survey-editor]'), options=root.querySelector('[data-survey-options]'), status=root.querySelector('[data-survey-admin-status]'), list=root.querySelector('[data-survey-admin-list]');
    function add(o) { var row=document.createElement('div'); row.className='cluster'; row.innerHTML='<input data-option-id type="hidden" value="'+esc(o&&o.id)+'"><input data-option-label aria-label="Option label" maxlength="250" required value="'+esc(o&&o.label)+'" placeholder="Option"><label><input data-option-text type="checkbox" '+(o&&o.allowsText?'checked':'')+'> Short text</label><button class="btn btn--secondary btn--sm" type="button">Remove</button>'; row.lastChild.onclick=function(){row.remove();}; options.appendChild(row); }
    function clear(){form.reset();form.querySelector('[data-survey-id]').value='';options.innerHTML='';add();add();status.textContent='';}
    function render(surveys){list.innerHTML='';if(!surveys.length){list.textContent='No surveys yet.';return;}surveys.forEach(function(s){var item=document.createElement('article');item.className='card';item.innerHTML='<h3>'+esc(s.title)+'</h3><p>'+esc(s.startsOn)+' to '+esc(s.endsOn)+' · '+(s.multiple?'Multiple choice':'Single choice')+'</p><div class="cluster"><button class="btn btn--secondary btn--sm" type="button">Edit</button><button class="btn btn--danger btn--sm" type="button">Delete</button></div>';var buttons=item.querySelectorAll('button');buttons[0].onclick=function(){form.querySelector('[data-survey-id]').value=s.id;form.querySelector('[data-survey-title]').value=s.title;form.querySelector('[data-survey-question]').value=s.question;form.querySelector('[data-survey-start]').value=s.startsOn;form.querySelector('[data-survey-end]').value=s.endsOn;form.querySelector('[data-survey-multiple]').checked=s.multiple;form.querySelector('[data-survey-results]').checked=s.showResults;options.innerHTML='';s.options.forEach(add);window.scrollTo({top:0,behavior:'smooth'});};buttons[1].onclick=function(){if(confirm('Delete this survey? Existing responses will no longer be visible.'))request('/api/admin/surveys?id='+encodeURIComponent(s.id),'DELETE').then(load).catch(function(e){status.textContent=e.message;});};list.appendChild(item);});}
    function load(){request('/api/admin/surveys','GET').then(function(d){render(d.surveys);}).catch(function(e){list.textContent=e.message;});} root.querySelector('[data-survey-add-option]').onclick=function(){add();};root.querySelector('[data-survey-new]').onclick=clear;root.querySelector('[data-survey-refresh]').onclick=load;
    form.onsubmit=function(e){e.preventDefault();var payload={id:form.querySelector('[data-survey-id]').value,title:form.querySelector('[data-survey-title]').value,question:form.querySelector('[data-survey-question]').value,startsOn:form.querySelector('[data-survey-start]').value,endsOn:form.querySelector('[data-survey-end]').value,multiple:form.querySelector('[data-survey-multiple]').checked,showResults:form.querySelector('[data-survey-results]').checked,options:Array.prototype.map.call(options.children,function(x){return{id:x.querySelector('[data-option-id]').value,label:x.querySelector('[data-option-label]').value,allowsText:x.querySelector('[data-option-text]').checked};})};request('/api/admin/surveys',payload.id?'PUT':'POST',payload).then(function(){status.textContent='Survey saved.';clear();load();}).catch(function(err){status.textContent=err.message;});};clear();load(); }
  function setupMember(root){function render(all){root.innerHTML='';if(!all.length){root.textContent='There are no surveys at the moment.';return;}all.forEach(function(result){var s=result.survey, article=document.createElement('article');article.className='dashboard-panel';var disabled=!result.canRespond?'disabled':'';article.innerHTML='<div class="dashboard-panel__header"><div><h2 class="dashboard-panel__title">'+esc(s.title)+'</h2><p>'+esc(s.question)+'</p></div></div><p class="form-hint">Open '+esc(s.startsOn)+' to '+esc(s.endsOn)+(result.canRespond?'':' · This survey is closed')+'</p><form class="stack"></form><div data-results></div><p role="status" aria-live="polite"></p>';var form=article.querySelector('form');s.options.forEach(function(o){var selected=(result.myOptionIds||[]).indexOf(o.id)>-1;form.insertAdjacentHTML('beforeend','<label><input type="'+(s.multiple?'checkbox':'radio')+'" name="survey" value="'+esc(o.id)+'" '+(selected?'checked':'')+' '+disabled+'> '+esc(o.label)+'</label>'+(o.allowsText?'<textarea data-other aria-label="Describe '+esc(o.label)+'" maxlength="250" rows="2" placeholder="Please describe (250 characters)" '+disabled+'>'+esc(result.myOtherText)+'</textarea>':''));});if(result.canRespond)form.insertAdjacentHTML('beforeend','<button class="btn btn--primary" type="submit">Save response</button>');function results(){var box=article.querySelector('[data-results]');if(!s.showResults){box.textContent='Aggregate results are not available for this survey.';return;}box.innerHTML='<h3>Results ('+result.total+' response'+(result.total===1?'':'s')+')</h3>'+s.options.map(function(o){return '<p>'+esc(o.label)+': <strong>'+((result.counts&&result.counts[o.id])||0)+'</strong></p>';}).join('');}results();form.onsubmit=function(e){e.preventDefault();var selected=Array.prototype.map.call(form.querySelectorAll('input:checked'),function(x){return x.value;}), other=form.querySelector('[data-other]');request('/api/member/survey-response','POST',{surveyId:s.id,optionIds:selected,otherText:other?other.value:''}).then(function(){load();}).catch(function(err){article.querySelector('[role=status]').textContent=err.message;});};root.appendChild(article);});}function load(){request('/api/member/surveys','GET').then(function(d){render(d.surveys);}).catch(function(e){root.textContent=e.message;});}load();}
  document.querySelectorAll('[data-survey-admin]').forEach(setupAdmin);document.querySelectorAll('[data-member-surveys]').forEach(setupMember);
}());
